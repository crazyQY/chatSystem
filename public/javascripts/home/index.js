var index = {
  showLoginError: function () {
    /*var paramsStr = location.search.substring(1);
    var params = paramsStr.split('&');
    var errMsg = '';
    params.forEach(function (pa, i) {
      if(/^le=/.test(pa)) {
        errMsg=pa.split('=')[1];
        return false;
      }
    });
    if(errMsg) {
      alert(errMsg);
    }*/
  },
  init: function () {
    this.showLoginError();
  }
};